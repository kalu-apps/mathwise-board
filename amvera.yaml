meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npm run build:showcase

run:
  command: PORT=4173 npm run backend:start:showcase
  containerPort: 4173
